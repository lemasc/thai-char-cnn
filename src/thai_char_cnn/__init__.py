"""Shared code for the split (03) and training (04) notebooks.

Notebooks own narrative and decisions; this package owns the logic they share, so a later
notebook (e.g. the augmentation preview) imports the same functions instead of copying them.
"""
